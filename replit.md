# Photos

React, TypeScript, and Vite photo gallery application.

## Run

- Use the `Start application` workflow.
- The workflow runs `npm run dev` and serves the app on port 5000.
- No secrets or external services are required.

## Checks

- `npm run lint` runs the TypeScript type check.
- `npm run build` creates a production build.